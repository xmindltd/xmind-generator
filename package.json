{
  "name": "xmind-generator",
  "version": "0.0.0",
  "main": "./src/index.js",
  "repository": "https://github.com/xmindltd/xmind-generator.git",
  "author": "Xmind Ltd.",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "npm run prettier && rm -rf lib && tsc --build",
    "prettier": "prettier --write \"src/**/*.ts\""
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "prettier": "^2.8.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.4"
  }
}
